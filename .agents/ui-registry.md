# ui-registry.md — TECIM Design Tokens

Captured by the `imprint` skill after every UI component. Design tokens are **code**, never CMS-editable — the admin edits text and images only.

## Baseline (pending)

No components built yet. Populate after the first component is implemented:

- Color palette / gradients / glass effects / overlays
- Typography scale (sizes, weights, line heights)
- Spacing tokens, section gutters, breakpoints
- Motion: timing, easing, scroll effects, transitions, loading/film-strip animations
- Patterns: buttons, cards, section shells, hero treatment
